//Arrow function declaration
// const add = (parameter1, parameter2) => parameter1 + parameter2;
// add(2,3); //=> 5

const twoAdder = x => x + 2;
// is the same as
const twoAdder1 = (x) => x + 2;

const divide = (a, b) => a / b;
const square = x => x * x;
const add = (a, b) => a + b;